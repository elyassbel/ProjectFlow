{%- if path.edit is defined and path.edit -%}
    <a class="p-1 text-dark" title="{{ 'btn.edit'|trans }}" href="{{ path(path.edit, {id: row.id}) }}"><i class="fas fa-pen"></i></a>
{%- endif -%}

{%- if path.show is defined and path.show -%}
    <a class="p-1 text-dark" title="{{ 'btn.show'|trans }}" href="{{ path(path.show, {id: row.id}) }}"><i class="far fa-eye"></i></a>
{%- endif -%}

{#{%- if path.delete is defined and app.user.id != row.id -%}#}
{#    <form method="post" action="{{ path(path.delete, {id: row.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">#}
{#        <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ row.id) }}">#}
{#        <button class="btn">{{ 'btn.delete'|trans }}</button>#}
{#    </form>#}
{#{%- endif -%}#}

{#{%- if impersonate == true and app.user.id != row.id and is_granted('ROLE_ALLOWED_TO_SWITCH') -%}#}
{#    <a class="p-1 text-primary" href="{{ path('app_dashboard', {_switch_user: row.email}) }}"><i class="far fa-smile"></i> As user</a>#}
{#{%- endif -%}#}